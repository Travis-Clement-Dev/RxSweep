# RxSweep Phase 3 Plan — Regulatory-Instrument UX, Action Queue, Export Suite

> **For agentic workers:** REQUIRED SUB-SKILL: superpowers:executing-plans. Steps use checkbox syntax. Baseline: tag `v0.1.0`. All work on branch `phase-3/regulatory-instrument-ux` via draft PR.

**Goal:** Replace the "competent generic" dashboard with a regulatory-instrument UX (worklist-first), relocate AI narrative to its native registers, and ship the export suite pharmacy informaticists actually exchange.

**Decisions already made (2026-07-09 discussion):**
- Direction: regulatory instrument (worklist/action-queue hero, USWDS-inspired document language). Summary leaves the dashboard: on-demand via chat "Brief me"; formal memo in exports.
- Chat stays docked right (current enterprise convention) but gains suggested actions.
- Exports: CSV, XLSX, **Markdown (AI-interchange)**, print-PDF memo. Export menu replaces the bare "download report" link.
- **Comp gate:** 3 design comps with real sweep data reviewed by Travis before any app code. **Fallback:** if comps miss his bar → HeroUI (MIT, React+Tailwind) as component layer, same tokens; queue/exports/chat/writing tasks proceed unchanged.

## Global constraints

Engine untouched (`matching.py`, `triage.py`, `sources/`). Product writing: FDA register, no em dashes, no markdown in generated text. WCAG 2.1 AA. Severity colors reserved for severity. Existing 71 tests stay green. Conventional commits on the branch.

---

### P3-1: Design comps (GATE)
- [x] `design/comps/index.html` — one page, variant switcher, real findings from run 20260709T231706Z: **A "Federal Register"** (paper document aesthetic, square corners, dense bordered tables, action-queue hero) · **B "Clinical console"** (left rail, instrument density, keyboard row focus, stat strip) · **C "Memo-first"** (interactive institutional memo with embedded queue/exhibits).
- [x] Publish as Artifact; commit; open **draft PR** to main; PR comment (Travis's account, authorized) presenting the variants. **PAUSE for pick or HeroUI fallback call.**

### P3-2: Export suite (library-agnostic; may start while gate pends)
- [x] `src/rxsweep/exports.py`: `write_exports(result: SweepResult, run_dir: Path) -> dict[str, Path]` producing `findings.csv` (stdlib), `findings.xlsx` (openpyxl; severity-tinted rows, frozen header, autofilter), `findings.md` (metadata header; per-finding: citation, item, NDC, source, severity, rationale, FDA query URL; provenance + disclaimer footer).
- [x] Memo restructure of `report.html.j2`: institutional memo format (header block TO/FROM/DATE/RE, purpose statement, summary section, findings exhibits, quarantine/unchecked disclosures, verification line) + `@media print` stylesheet (print → clean PDF). Summary appears here, not the dashboard.
- [x] Pipeline writes all exports per run; CLI prints paths; server `GET /api/sweeps/{id}/export/{fmt}` (`csv|xlsx|md|report`) with correct content-disposition.
- [x] `tests/test_exports.py` + server endpoint tests.

### P3-3: Worklist rebuild (after gate)
- [x] Port chosen comp language into `web/src/theme.css` + components (or HeroUI adoption per fallback).
- [x] Action-queue hero: verb-led items derived from findings (`Verify lots for {item} — {class}, {n} records`), severity-ordered, check-off (local state), queue item click = drawer.
- [x] Findings table: sorting + filter row (severity/source/label); summary card removed from dashboard.
- [x] Export menu in header (4 formats + audit log note).

### P3-4: Chat "Brief me"
- [x] Suggested-action chips; "Brief me for the huddle" renders `result.summary` instantly (no API call), citations linkified; 2–3 other suggested prompts via `chat_reply`.

### P3-5: Writing-governance pass
- [x] Sweep all generated-artifact copy (memo, export headers, UI strings) for register compliance; update SYSTEM_CARD (summary placement, exports) + GOVERNANCE (export artifacts list).

### P3-6: Verify + finish PR
- [x] `uv run pytest` green; `npm run build`; wheel check (static + sample present; exports importable).
- [x] Browser: full journey, all four export downloads, "Brief me", both themes, keyboard pass.
- [x] PR: before/after screenshots, verification notes, mark ready for review. Travis merges.
